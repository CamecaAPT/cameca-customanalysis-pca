<UserControl x:Class="Cameca.CustomAnalysis.Pca.ComponentsTileView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Cameca.CustomAnalysis.Pca"
             xmlns:interface="clr-namespace:Cameca.CustomAnalysis.Interface;assembly=Cameca.CustomAnalysis.Interface"
             xmlns:controls="clr-namespace:Cameca.Extensions.Controls;assembly=Cameca.Extensions.Controls"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <local:SingleRenderDataValueConverter x:Key="SingleRenderDataValueConverter" />
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal">
            <CheckBox Content="Sync Charts" IsChecked="{Binding Sync, RelativeSource={RelativeSource AncestorType=local:ComponentsTileView}}" />
            <CheckBox Content="Scale Bar" IsChecked="{Binding ScaleBarVisible, RelativeSource={RelativeSource AncestorType=local:ComponentsTileView}}" />
        </StackPanel>
        <ItemsControl x:Name="ComponentsItemControl"
                      Grid.Row="1"
                      ItemsSource="{Binding ItemsSource, RelativeSource={RelativeSource AncestorType=local:ComponentsTileView}}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid Columns="{Binding Columns, RelativeSource={RelativeSource AncestorType=local:ComponentsTileView}}"
                                 Rows="{Binding Rows, RelativeSource={RelativeSource AncestorType=local:ComponentsTileView}}" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate DataType="{x:Type interface:IRenderData}">
                    <GroupBox Header="{Binding Name}">
                        <controls:Chart3D DataSource="{Binding Converter={StaticResource SingleRenderDataValueConverter}}"
                                          IsScaleBarVisible="{Binding ScaleBarVisible, RelativeSource={RelativeSource AncestorType=local:ComponentsTileView}}"
                                          IsToolbarVisible="False" />
                    </GroupBox>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</UserControl>
