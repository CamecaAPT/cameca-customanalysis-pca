<UserControl x:Class="Cameca.CustomAnalysis.Pca.PcaView"
             x:ClassModifier="internal"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
             xmlns:local="clr-namespace:Cameca.CustomAnalysis.Pca"
             xmlns:utils="clr-namespace:Cameca.CustomAnalysis.Utilities.Controls;assembly=Cameca.CustomAnalysis.Utilities"
             xmlns:controls="clr-namespace:Cameca.Extensions.Controls;assembly=Cameca.Extensions.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             DataContext="{Binding Node}"
             d:DataContext="{d:DesignInstance Type={x:Type local:PrincipalComponentAnalysisOld}}">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </UserControl.Resources>

    <utils:ButtonOverlayControl ButtonContent="Update"
                                ButtonCommand="{Binding UpdateCommand}"
                                OverlayVisibility="{Binding UpdateCommandCanExecute, Converter={StaticResource BooleanToVisibilityConverter}}">

        <TabControl>
            <TabItem Header="Noise Eigenvalues">
                <utils:ButtonOverlayControl ButtonCommand="{Binding UpdateRankEstimationCommand}"
                                            ButtonContent="Update"
                                            OverlayVisibility="{Binding UpdateRankEstimationCanExecute, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <Grid>
                        <controls:Chart2D DataSource="{Binding EigenvalueRenderData}"
                                          IsLogScaleY="{Binding Properties.LogScaleY, Mode=TwoWay}"
                                          AxisXLabel="Sorted eigenvalue index"
                                          AxisYLabel="Eigenvalue"/>
                    </Grid>
                </utils:ButtonOverlayControl>
            </TabItem>
            
            <TabItem Header="Components">
                <utils:ButtonOverlayControl ButtonCommand="{Binding UpdateComponentsCommand}"
                                ButtonContent="Update"
                                OverlayVisibility="{Binding UpdateComponentsCanExecute, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <local:ComponentsTileView Grid.Column="0"
                                                  Grid.Row="0"
                                                  Grid.RowSpan="3"
                                                  IsPcaPhases="false"
                                                  ItemsSource="{Binding ComponentRenderData}" />
                        <Grid Grid.Column="1"
                              Grid.Row="0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Label Grid.Column="0" Target="ColorMapMaxTextBox" Content="Max" Padding="0 0 8 0" />
                            <TextBox Grid.Column="1" x:Name="ColorMapMaxTextBox">
                                <TextBox.Text>
                                    <Binding Path="ComponentsColorMap.TopValue"
                                             UpdateSourceTrigger="PropertyChanged"
                                             Delay="500">
                                        <Binding.ValidationRules>
                                            <local:NumericValidationRule />
                                        </Binding.ValidationRules>
                                    </Binding>
                                </TextBox.Text>
                            </TextBox>
                        </Grid>

                        <controls:ColorMapEditor Grid.Column="1"
                                                 Grid.Row="1"
                                                 ColorMap="{Binding ComponentsColorMap}"
                                                 ValueLabel="Score" />

                        <Grid Grid.Column="1"
                              Grid.Row="2">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Label Grid.Column="0" Target="ColorMapMinTextBox" Content="Min" Padding="0 0 8 0" />
                            <TextBox Grid.Column="1" x:Name="ColorMapMinTextBox">
                                <TextBox.Text>
                                    <Binding Path="ComponentsColorMap.BottomValue"
                                             UpdateSourceTrigger="PropertyChanged"
                                             Delay="500">
                                        <Binding.ValidationRules>
                                            <local:NumericValidationRule />
                                        </Binding.ValidationRules>
                                    </Binding>
                                </TextBox.Text>
                            </TextBox>
                        </Grid>
                    </Grid>
                </utils:ButtonOverlayControl>
            </TabItem>

            <TabItem Header="Scores">
                <utils:ButtonOverlayControl ButtonCommand="{Binding UpdateComponentsCommand}"
                            ButtonContent="Update"
                            OverlayVisibility="{Binding UpdateComponentsCanExecute, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="120" />
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <ListBox x:Name="ComponentSelector"
                                 Grid.Column="0"
                                 DisplayMemberPath="Name"
                                 SelectedIndex="{Binding SelectedLoadingsIndex}"
                                 ItemsSource="{Binding PcaComponentsResults.Components}" />
                        <controls:Chart2D x:Name="ScoresHistogram" 
                                          DataSource="{Binding ScoresHistogramRenderData}"
                                          Grid.Column="1"
                                          AxisXLabel="Score"
                                          IsLegendVisible="False"
                                          AxisYLabel="Voxels per PCA Unit"/>
                    </Grid>
                </utils:ButtonOverlayControl>
            </TabItem>

            <TabItem Header="Loading">
                <utils:ButtonOverlayControl ButtonCommand="{Binding UpdateComponentsCommand}"
                                            ButtonContent="Update"
                                            OverlayVisibility="{Binding UpdateComponentsCanExecute, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="120" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="36" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>
                        <ListBox x:Name="LoadingsData"
                                 Grid.Column="0"
                                 Grid.Row="0"
                                 Grid.RowSpan="2"
                                 DisplayMemberPath="Name"
                                 SelectedIndex="{Binding SelectedLoadingsIndex}"
                                 ItemsSource="{Binding PcaComponentsResults.Components}" />

                        <Label x:Name="GridLabel" 
                            Grid.Column="1" 
                            Grid.Row="0" 
                            Content="{Binding LoadingsChartTitle}"
                            Height="32" 
                            Width="280" 
                            FontSize="16" 
                            FontFamily="Calibri"  
                            FontWeight="Bold"   
                            Background="White"   
                            Foreground="Black">
                        </Label>
                        <lvc:CartesianChart Grid.Column="1"
                                            Grid.Row="1"
                                            Series="{Binding LoadingsSeries}"
                                            DisableAnimations="True"
                                            LegendLocation="None"
                                            Visibility="{Binding LoadingHistogramRenderData, Converter={local:CollectionVisibilityConverter Invert=True}}">
                            <lvc:CartesianChart.Resources>
                                <Style TargetType="lvc:DefaultTooltip">
                                    <Setter Property="ShowSeries" Value="False" />
                                </Style>
                            </lvc:CartesianChart.Resources>
                            <lvc:CartesianChart.AxisX>
                                <lvc:Axis Labels="{Binding LoadingsLabels}"
                                            LabelsRotation="{Binding LoadingsLabelsRotation}"
                                            FontSize="18">
                                    <lvc:Axis.Separator>
                                        <lvc:Separator Step="1" />
                                    </lvc:Axis.Separator>
                                </lvc:Axis>
                            </lvc:CartesianChart.AxisX>
                            <lvc:CartesianChart.AxisY>
                                <lvc:Axis Title="Loadings" FontSize="18" LabelFormatter="{Binding AxisYLabelFormatter, Mode=OneTime}" />
                            </lvc:CartesianChart.AxisY>
                        </lvc:CartesianChart>

                        <controls:Chart2D Grid.Column="1"
                                          Grid.Row="1"
                                          Grid.RowSpan="2"
                                          DataSource="{Binding LoadingHistogramRenderData}"
                                          AxisYLabel="Loadings"
                                          AxisXLabel="Bin Index"
                                          Visibility="{Binding LoadingHistogramRenderData, Converter={local:CollectionVisibilityConverter}}" />
                    </Grid>
                </utils:ButtonOverlayControl>
            </TabItem>

        </TabControl>
    </utils:ButtonOverlayControl>
</UserControl>
